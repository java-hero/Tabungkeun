<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.d3ifcool.tabungkeun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TambahTabunganActivity" android:theme="@style/TemaUtama"></activity>
        <activity android:name=".TambahPengeluaranActivity" android:theme="@style/TemaUtama"></activity>
        <activity android:name=".TambahPemasukanActivity" android:theme="@style/TemaUtama">
        </activity>
        <activity
            android:name=".TambahWishlistActivity"
            android:theme="@style/TemaUtama" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/TemaLogin">
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/TemaUtama">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>